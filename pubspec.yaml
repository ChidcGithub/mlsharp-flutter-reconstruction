name: mlsharp_flutter
description: "MLSharp 3D Maker - Flutter Reconstruction for Snapdragon GPU Acceleration"
publish_to: 'none'

version: 0.0.1-preview.4+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  dio: ^5.3.0
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  image_picker: ^1.0.0
  logger: ^2.0.0
  json_serializable: ^6.7.0
  model_viewer_plus: ^1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
